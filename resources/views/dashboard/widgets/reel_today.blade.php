{{-- Reel Planner — Today. Its own file at dashboard/_reel-today.blade.php
     (it predates the widget system and stays a standalone include -- see
     that file's own doc block for why: its own fetch(), own Notion
     refresh, nothing this widget wrapper needs to touch). --}}
@include('dashboard._reel-today')
